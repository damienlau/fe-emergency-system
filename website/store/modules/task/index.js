import eventModule from "./event";

const state = () => ({});
const getters = {};
const actions = {};
const mutations = {};
const modules = { eventModule };

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
  modules,
};
