// 组件-卡片

import { defineComponent } from "vue";

export default defineComponent({
  name: "Card",
  props: {},
  setup(props, { slots, emit }) {
    <div class="bg-navy-2 hover:bg-navy-1">
      {/* 卡片头部 */}
      <div class="px-16"></div>
      {/* 卡片主体 */}
    </div>;
  },
});
